# 学习会话记录

- **日期**：2026-04-09
- **会话编号**：001
- **领域**：04-分布式系统
- **子模块**：分布式事务（MQ 边界） + 分布式锁（库存分桶最小实现）
- **起始等级**：L1（初阶架构）

---

## 教学启动

- **承接内容**：延续 `2026-04-08_001` 的分布式锁学习，先回补分布式事务中的模型边界澄清。
- **教学目标**：收住“MQ 最终一致性”与 `Saga/TCC` 的关系，避免后续选型时模型混用。
- **本轮节奏**：边界提问 -> 术语澄清 -> 口径固定。

---

## 第7节：分布式事务边界澄清（1题）

### 学生提问摘要

1. 提出问题：`基于消息队列的最终一致性属于 Saga + TCC 吗？`
2. 关注点聚焦在“方案归类”与“是否必须组合使用”。

### 评判结果

- **评判结果**：✅ 正确（问题抓到关键边界）
- **得分**：9.3/10
- **评分明细**：
  - 正确性：2.8/3
  - 深度：2.7/3
  - 广度：1.8/2
  - 实战感：2/2

### 三项反馈

- **正确点**：
  1. 你识别到了一个高频误区：把“可靠消息最终一致性”直接等同于 `Saga + TCC`；
  2. 你关注的是“分类边界”而非单点技巧，这对架构选型很关键；
  3. 你接受了并列关系口径：`MQ 最终一致性`、`Saga`、`TCC` 是不同治理手段。
- **风险点**：
  1. 若把三者强行绑定，会在简单链路中过度设计，增加实现复杂度；
  2. 若只讲“异步最终一致”但缺少幂等、重试、死信和补偿闭环，生产可用性会明显不足。
- **最小修正建议**：
  1. 固定一句：`MQ 最终一致性是消息可靠投递与异步收敛机制，不等于 Saga + TCC。`
  2. 固定一句：`Saga 管流程补偿，TCC 管关键资源预留，是否组合取决于链路风险与一致性等级。`

---

## 本次结论

- **收官结果**：✅ 通过
- **最终得分**：9.3/10
- **固定口径**：
  1. `基于 MQ 的最终一致性` 与 `Saga/TCC` 属于并列方案，不是从属关系。
  2. `Saga` 可通过事件编排/协同落地，常与消息机制配合，但并不要求 `TCC` 必选。
  3. 事务方案应按“资源类型、失败代价、时延目标、可补偿性”组合设计，而非套模板。

---

## 第8节：分布式锁最小实现设计（1题）

### 学生作答摘要

1. 给出两类扣减 SQL 思路：单表模型与按桶拆分模型，并引入 `token` 条件；
2. 幂等键设计为 `order_id + action`；
3. 失败分流按“重试 / 直接拒绝 / 进入补偿”三类给出基础判定逻辑。

### 评判结果

- **评判结果**：✅ 正确（方向正确，工程细节需收紧）
- **得分**：8.8/10
- **评分明细**：
  - 正确性：2.6/3
  - 深度：2.4/3
  - 广度：1.8/2
  - 实战感：2/2

### 三项反馈

- **正确点**：
  1. 你已经把“原子扣减 + 幂等 + 失败分流”这三根主线搭起来了；
  2. 你能区分“业务确定性失败”和“异常失败”，这对重试策略非常关键；
  3. 你把补偿放在链路后段考虑，具备完整事务视角。
- **风险点**：
  1. SQL 目前是语义描述，不是可直接执行的标准写法；`token` 取模选桶也容易造成模型耦合；
  2. `order_id + action` 作为幂等键粒度偏粗，遇到多资源/多尝试会冲突；
  3. “验证通过就补偿”条件过宽，补偿应限定在“局部成功后下游失败”场景。
- **最小修正建议**：
  1. 扣减 SQL 固定为：`按 sku_id + bucket_id 定位`、`version/token 防旧写`、`available>=qty` 原子判断；
  2. 幂等键固定为：`order_id + action + resource_id (+ attempt_no)`；
  3. 失败分流固定为：`确定性失败 -> 直接拒绝`，`瞬时失败 -> 有界重试`，`局部成功后失败 -> 触发补偿`。

## 第8节结论

- **收官结果**：✅ 通过（需继续收敛 SQL 与幂等键细节）
- **最终得分**：8.8/10
- **固定口径**：
  1. `fencing/version` 用于防旧写，不用于主路径做流量分配。
  2. `bucket` 应由稳定业务键路由（如 `order_id/user_id/sku_id` hash），不是由 `token` 决定。
  3. 补偿触发前提是“前序步骤已成功落地且后续步骤失败”，不是“只要验证通过”。

---

## 第9节：`UPDATE=0` 失败分流顺序（1题）

### 学生作答摘要

1. 提出三类结果：单桶不足重试、全桶不足拒绝、其余拒绝；
2. 认为“全部为 0 不会是幂等成功”。

### 评判结果

- **评判结果**：⚠️ 部分正确（分类框架有了，判定顺序不正确）
- **得分**：6.8/10
- **评分明细**：
  - 正确性：1.8/3
  - 深度：1.8/3
  - 广度：1.4/2
  - 实战感：1.8/2

### 三项反馈

- **正确点**：
  1. 你已经形成“重试 / 拒绝 / 幂等”三分流意识，这是对的；
  2. 你能区分“局部不足”和“全局不足”的业务后果，具备容量视角。
- **风险点**：
  1. 在固定路由分桶模型下，失败后随意换桶会破坏并发语义，导致超卖风险；
  2. `UPDATE=0` 可能是“重复请求已成功落地（幂等成功）”，不能直接判拒绝。
- **最小修正建议**：
  1. 固定顺序先查幂等记录，再查确定性业务失败，再判瞬时异常可重试；
  2. 固定一句：`路由桶是稳定映射，不是失败后的兜底逃生口。`

## 第9节结论

- **收官结果**：❌ 未通过（需重答）
- **最终得分**：6.8/10
- **固定口径**：
  1. `UPDATE=0` 第一判断不是“库存不足”，而是“是否幂等已成功”。
  2. `bucket` 固定映射，不做任意跨桶重试。
  3. “可重试”仅针对瞬时失败（超时、死锁、网络抖动）且必须有上限。

---

## 第10节：幂等表最小字段（1题）

### 学生作答摘要

1. 给出字段集合：主键、订单ID、库存ID、`token`、预占数量、创建时间、处理结果、订单状态、库存状态；
2. 补充索引建议：`order_id + inventory_id`。

### 评判结果

- **评判结果**：⚠️ 部分正确（有结构意识，但“最小字段”未收敛）
- **得分**：7.9/10
- **评分明细**：
  - 正确性：2.2/3
  - 深度：2.1/3
  - 广度：1.6/2
  - 实战感：2/2

### 三项反馈

- **正确点**：
  1. 你已经覆盖了资源定位与处理结果，这对幂等落盘是关键；
  2. 你主动给出复合索引，体现了性能与唯一性意识；
  3. 你把 `token/version` 也放进视角，有助于防旧写治理。
- **风险点**：
  1. 最关键的 `action`（如 reserve/cancel/refund）缺失，无法区分同订单不同动作；
  2. 缺少统一 `idempotency_key/request_id`，跨系统重放时会难以对齐；
  3. 字段过多且含业务状态字段，偏离“最小 6 字段”的目标。
- **最小修正建议**：
  1. 先固定 6 字段最小集合，再按场景扩展可选字段；
  2. 组合唯一键建议优先：`(idempotency_key)` 或 `(order_id, action, resource_id)`。

## 第10节结论

- **收官结果**：❌ 未通过（需按“最小 6 字段”重答）
- **最终得分**：7.9/10
- **固定口径**：
  1. 幂等表首先解决“唯一识别一次动作”，其次才是业务扩展字段。
  2. `action` 是不可省字段，否则同订单多动作会混淆。
  3. “最小设计”优先可判重、可回放、可审计，不追求一次塞全业务状态。

---

## 第11节：幂等键边界复答（1题）

### 学生作答摘要

1. 明确补充 `action` 并删除订单/库存状态字段；
2. 仍认为 `inventory_id` 可代表 `idempotency_key`。

### 评判结果

- **评判结果**：⚠️ 部分正确（方向继续改善，但核心边界仍误）
- **得分**：8.1/10
- **评分明细**：
  - 正确性：2.3/3
  - 深度：2.2/3
  - 广度：1.6/2
  - 实战感：2/2

### 三项反馈

- **正确点**：
  1. 你主动按“最小字段”目标做了减法，说明收敛意识到位；
  2. 你补上 `action`，已经修复了同订单多动作混淆问题的一半。
- **风险点**：
  1. `inventory_id` 不是请求唯一键；同一库存会承载大量订单和动作，无法判重“某一次请求”；
  2. 若以 `inventory_id` 判重，会把不同订单请求误判为重复，造成漏扣或错拒。
- **最小修正建议**：
  1. 固定使用独立请求键：`idempotency_key`（建议上游传 `request_id`）；
  2. 无全局键时，至少唯一约束：`(order_id, action, resource_id)`。

## 第11节结论

- **收官结果**：❌ 未通过（需给出最终 6 字段清单）
- **最终得分**：8.1/10
- **固定口径**：
  1. `resource_id` 只用于定位资源，不用于标识一次请求。
  2. 幂等判重对象是“请求”，不是“库存实体”。
  3. “最小 6 字段”必须包含请求唯一性字段。

---

## 第12节：最小DDL取舍确认（1题）

### 学生作答摘要

1. 说明当前阶段先聚焦字段口径，不再展开 DDL。

### 评判结果

- **评判结果**：✅ 通过
- **得分**：9.0/10

### 三项反馈

- **正确点**：
  1. 你能把注意力集中在“判重口径”主目标上，阶段控制合理。
- **风险点**：
  1. 后续落地时仍需补齐类型、索引和迁移细节。
- **最小修正建议**：
  1. 在进入实现题时一次性补齐 DDL 细节，不提前发散。

## 第12节结论

- **收官结果**：✅ 通过
- **最终得分**：9.0/10

---

## 第13节：`status` 最小枚举（1题）

### 学生作答摘要

1. 回答为：`已经完成 / 可重试 / 已失败`。

### 评判结果

- **评判结果**：⚠️ 部分正确
- **得分**：8.0/10
- **评分明细**：
  - 正确性：2.2/3
  - 深度：2.0/3
  - 广度：1.8/2
  - 实战感：2/2

### 三项反馈

- **正确点**：
  1. 你覆盖了终态（成功/失败）与重试语义，方向正确。
- **风险点**：
  1. 最小状态机缺少“处理中”状态，无法准确表示首次执行与并发中的请求；
  2. “可重试”通常更适合作为错误分类或策略标签，而不是幂等主状态。
- **最小修正建议**：
  1. 最小枚举固定：`PROCESSING / SUCCESS / FAILED`。
  2. 重试能力通过 `error_code` 或 `next_retry_at` 等字段表达。

## 第13节结论

- **收官结果**：❌ 未通过（需重答）
- **最终得分**：8.0/10
- **固定口径**：
  1. 幂等状态机先保证“进行中/成功/失败”三态闭环。
  2. “可重试”是策略属性，不是主状态的必选枚举。

---

## 第14节：`PROCESSING` 命中返回语义（1题）

### 学生作答摘要

1. 建议对重复请求返回友好提示（如“请求太快，请稍后再试”）；
2. 认为客户端重复与后端内部重复可做不同处理；
3. 提到可按业务设计决定细节。

### 评判结果

- **评判结果**：⚠️ 部分正确
- **得分**：8.4/10
- **评分明细**：
  - 正确性：2.4/3
  - 深度：2.2/3
  - 广度：1.8/2
  - 实战感：2/2

### 三项反馈

- **正确点**：
  1. 你抓住了核心：`PROCESSING` 命中时不应直接当成功或失败；
  2. 你具备“按调用方差异化返回”的产品意识。
- **风险点**：
  1. 即使是后端内部重复命中，也不应再次执行主业务逻辑，否则会破坏幂等；
  2. 若只返回文案不定义机器可读语义（code/retry_after），调用方容易实现不一致。
- **最小修正建议**：
  1. 固定返回：`status=PROCESSING, code=IDEMPOTENT_IN_PROGRESS, retry_after=xxms`；
  2. 固定动作：调用方应“等待后回查结果”，而非再次执行业务。

## 第14节结论

- **收官结果**：❌ 未通过（需补机器可读返回口径）
- **最终得分**：8.4/10
- **固定口径**：
  1. 命中 `PROCESSING` = 返回处理中，不重入主逻辑。
  2. 无论外部还是内部调用，幂等主逻辑都不能二次执行。
  3. 返回需包含可自动化处理的字段（`code/retry_after`）。

---

## 第15节：`PROCESSING` 响应体实答（1题）

### 学生作答摘要

1. 给出 `code=1001` 与友好文案“请求太快，请稍后再试”；
2. 说明客户端收到后可提示用户并记录日志。

### 评判结果

- **评判结果**：⚠️ 部分正确
- **得分**：8.6/10
- **评分明细**：
  - 正确性：2.5/3
  - 深度：2.1/3
  - 广度：2.0/2
  - 实战感：2/2

### 三项反馈

- **正确点**：
  1. 你已经提供了机器码和用户提示，具备基础可用性；
  2. 你补充了客户端动作（提示+日志），工程意识不错。
- **风险点**：
  1. 仍缺少显式 `status=PROCESSING`，不利于调用方统一状态机；
  2. 缺少 `retry_after`，客户端无法稳定退避重试。
- **最小修正建议**：
  1. 最小三字段固定为：`status/code/retry_after`；
  2. `message` 可保留为可选人类可读字段，不替代机器字段。

## 第15节结论

- **收官结果**：❌ 未通过（差最后一步）
- **最终得分**：8.6/10
- **固定口径**：
  1. 机器可读优先：`status/code/retry_after`。
  2. 文案是辅助，不是协议主字段。

---

## 第16节：`retry_after` 是否应返回（1题）

### 学生作答摘要

1. 认为返回 `retry_after` 不安全；
2. 建议由客户端自行判断重试节奏。

### 评判结果

- **评判结果**：⚠️ 部分正确
- **得分**：8.2/10
- **评分明细**：
  - 正确性：2.3/3
  - 深度：2.1/3
  - 广度：1.8/2
  - 实战感：2/2

### 三项反馈

- **正确点**：
  1. 你有明确的安全意识，能主动考虑被探测与被滥用风险；
  2. 你意识到客户端具备本地退避能力，这是可行兜底。
- **风险点**：
  1. 完全不返回 `retry_after`，容易导致客户端重试策略发散，形成重试风暴；
  2. 只靠客户端猜测节奏，跨端实现会不一致，削弱系统可控性。
- **最小修正建议**：
  1. 折中方案：返回“粗粒度 `retry_after`”（如 200/500/1000ms 档位）；
  2. 配合随机抖动与上限，不暴露内部精细调度参数。

## 第16节结论

- **收官结果**：❌ 未通过（需给出折中安全方案）
- **最终得分**：8.2/10
- **固定口径**：
  1. `retry_after` 主要用于控节奏，默认不属于高敏感数据。
  2. 安全目标是“少暴露细节”，不是“完全不返回控制信号”。
  3. 服务端给粗粒度建议，客户端做兜底退避。

---

## 第17节：仅保留 `code` 的可行性（1题）

### 学生作答摘要

1. 主张仅返回 `code`，认为 `status` 与 `code` 对客户端等价；
2. 提出客户端固定等待后重试，并建议重新生成 `request_id`。

### 评判结果

- **评判结果**：⚠️ 部分正确
- **得分**：7.6/10
- **评分明细**：
  - 正确性：2.0/3
  - 深度：1.8/3
  - 广度：1.8/2
  - 实战感：2/2

### 三项反馈

- **正确点**：
  1. 你在追求协议简化，方向上有工程落地意识；
  2. 你意识到客户端可承担一部分重试控制。
- **风险点**：
  1. `status` 与 `code` 职责不同：`status` 负责状态机主语义，`code` 负责错误细分；仅 `code` 易造成跨端行为不一致；
  2. 命中 `PROCESSING` 后重生成 `request_id` 会绕过幂等保护，带来重复执行业务风险。
- **最小修正建议**：
  1. 固定最小协议：`status + code + retry_after(粗粒度)`；
  2. 固定规则：同一业务动作重试必须复用原 `request_id`，禁止重生成。

## 第17节结论

- **收官结果**：❌ 未通过（需收敛协议分层口径）
- **最终得分**：7.6/10
- **固定口径**：
  1. `status` 不等于 `code`，两者分层是为了解耦状态机与错误细分。
  2. 幂等重试必须复用同一 `request_id`。
  3. 协议可简但不能牺牲可控性与一致性。

---

## 第18节：`PROCESSING` 命中重试策略（1题）

### 学生作答摘要

1. 提出客户端固定等待后重试；
2. 表示若命中幂等键返回处理中码；
3. 同时提出重试可重新生成 `request_id`。

### 评判结果

- **评判结果**：⚠️ 部分正确
- **得分**：7.2/10
- **评分明细**：
  - 正确性：1.8/3
  - 深度：1.8/3
  - 广度：1.6/2
  - 实战感：2/2

### 三项反馈

- **正确点**：
  1. 你已明确“命中幂等键要返回处理中码”，方向正确；
  2. 你关注了客户端重试体验与节奏控制。
- **风险点**：
  1. 命中 `PROCESSING` 后若重生成 `request_id`，会绕过幂等保护，可能造成重复执行；
  2. 仅“固定等待重试”缺少结果回查机制，容易在高延迟场景下形成盲重试。
- **最小修正建议**：
  1. 固定规则：同一业务动作重试必须复用原 `request_id`；
  2. 增加“结果回查接口”，命中处理中优先回查而非盲重试。

## 第18节结论

- **收官结果**：❌ 未通过（需补回查能力定义）
- **最终得分**：7.2/10
- **固定口径**：
  1. 幂等重试“可等、可查、不可换键”。
  2. `request_id` 是同一动作的稳定标识，重试不应更换。
  3. 回查能力是无 `retry_after` 时的必需能力。

---

## 下一步

- 继续 `分布式锁`：定义一个最小结果回查接口（入参+出参）。

---

*保存时间：2026-04-09*
