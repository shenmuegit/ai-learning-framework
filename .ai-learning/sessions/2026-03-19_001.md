# 学习会话记录

- **日期**：2026-03-19
- **会话编号**：001
- **领域**：03-微服务架构
- **子模块**：服务通信（通信+治理+一致性联合）
- **起始等级**：L1（初阶架构）

---

## 问答记录

### Q1（L2 设计题 - 订单/库存/支付/营销联合方案）
- **题目类型**：设计题
- **难度**：L2
- **问题**：在订单超时、营销慢、支付偶发失败场景下，设计通信方式、超时重试、补偿链路与终态状态机。
- **学生回答摘要**：
  - 明确了“非核心链路拆出主调用链”，库存/营销同步校验，支付拆成独立支付行为；
  - 给出入口幂等（参数哈希）与用户维度分布式锁、防重策略；
  - 采用预占库存 + 支付成功后异步扣减/核销，并有回调+定时回查兜底；
  - 给出订单状态流转与人工兜底治理思路。
- **评判结果**：✅ 正确（补充后）
- **得分**：10/10（补充后）
- **评分明细**：
  - 正确性：3/3（预算、状态机与回查补充后主链路闭环完整）
  - 深度：3/3（支付失败补偿、库存状态显式化均已补齐）
  - 架构约束：2/2（同步/异步边界总体合理）
  - 实战感：2/2（具备可落地治理意识）
- **反馈（扣分点）**：
  1. 支付失败补偿链路建议继续按“至少4步”结构化展开；
  2. 库存状态机建议显式化（如 `RESERVED -> DEDUCTED/RELEASED`）便于审计与排障。
- **学生补充（追答）**：
  - 支付失败兜底：回调缺失由定时任务回查支付状态，成功后走与回调一致的订单更新与后续消息；
  - 支付状态机：`待支付 -> 已支付/已退款`；
  - 库存状态表达：预占库存与订单关联，支付成功转实扣，超时取消释放；
  - 超时预算：库存与营销调用超时 2 秒，重试最多 3 次（可配置）。
- **导师复评**：
  - 本次补充已补齐预算数值与多服务状态表达，分数上调到 `9/10`；
  - 距满分仍差一步：把“支付失败补偿”明确写成4步链路（触发、动作、补偿、终态）。
- **学生补充（二次追答）**：
  - 支付失败补偿4步链路：
    1. 触发：定时任务触发；
    2. 动作：定时任务主动查询第三方支付状态；
    3. 补偿：查询成功后走支付成功接口，修改订单为待发货并发送库存/优惠券消息；
    4. 终态：订单/库存/优惠券收敛到最终一致。
- **导师二次复评**：
  - 本次补充已满足“4步补偿链路”要求；
  - 当前剩余改进点为“库存状态机显式化表达”（如 `RESERVED -> DEDUCTED/RELEASED`），总分暂维持 `9/10`。
- **学生补充（三次追答）**：
  - 补充库存状态机显式化：预占库存关联订单记录增加状态（`预占`、`已占`），支付成功后由库存消费者推进状态。
- **导师三次复评**：
  - 本次补充补齐最后缺口，库存状态机已可审计、可追踪；
  - 总分上调为 `10/10`。
- **学生补充（四次追答）**：
  - 优惠券消费者：
    1. 使用消息队列异步核销，按 `order_id` 做数据库幂等；
    2. 失败重试并设置上限，超限后记录警告日志并转人工介入；
    3. 订单侧已完成规则校验后，进入核销阶段以“支付成功事实”为准；
    4. 可重复使用券按“次数/金额上限”更新券状态。
  - 库存消费者：
    1. 支付成功后异步执行“预占转实扣”；
    2. 以 `order_id` 做幂等，已实扣则忽略重复消息；
    3. 重试超阈值后告警并人工介入；
    4. 增加库存-订单关联记录，状态显式为 `预占/实扣`。
  - 网关路由：
    - 采用 `user_id` 取模/哈希分桶做粘性路由，保证同一用户稳定落到固定节点。
  - 告警日志：
    - 字段包含 `request_id`、`order_id`、业务编码、时间、原因、级别、消费者参数、异常堆栈。
- **导师同步说明**：
  - 已按“回答了什么就同步什么”归档本次追答内容；
  - 本次为同题扩展补充，不新增题目计数。
- **知识点标签**：服务通信, 幂等防重, 支付回查, 最终一致性, 状态机, 超时预算

### Q2（L2 设计题 - 超时取消与支付回调并发裁决）
- **题目类型**：设计题
- **难度**：L2
- **问题**：订单超时取消与支付成功回调并发到达时，如何避免重复释放或误实扣，并给出条件更新SQL、三方状态机、幂等去重与超时重试数值。
- **学生回答摘要**：
  - 给出订单超时与支付回调并发场景，并提出“订单维度分布式锁（`order_id`）”做互斥；
  - 给出两种业务策略：超时后退款并回滚，或支付成功覆盖取消；
  - 给出“预占 -> 实扣”的一组SQL草案；
  - 其余部分（状态机、幂等去重、超时重试）说明“之前已回答，不再重复”。
- **评判结果**：✅ 正确（补充后）
- **得分**：10/10（补充后）
- **评分明细**：
  - 正确性：3/3（实扣与释放链路方向、并发保护均已补齐）
  - 深度：3/3（三方状态机、幂等去重键、预算与重试上限均已补齐）
  - 架构约束：2/2（单一终态规则明确，跨服务一致性边界清晰）
  - 实战感：2/2（具备可执行SQL、锁策略、重试与审计意识）
- **反馈（扣分点）**：
  1. `预占 -> 实扣` SQL 中实库存更新方向写反，实扣应为 `inventory = inventory - qty`，不是增加；
  2. 缺少 `预占 -> 释放` 的条件更新SQL；
  3. 并发裁决未落到“单一终态规则”，需要明确“超时后支付成功”到底进入“退款分支”还是“恢复履约分支”，不可两条同时作为默认；
  4. 本题要求的三方状态机、幂等去重主键/唯一键、超时与重试上限未在本次作答中展开。
- **学生补充（Q2追答）**：
  - 更正了 `预占 -> 实扣` 的库存方向：`inventory = inventory - qty`，并同步减少预占库存；
  - 补充了 `预占 -> 释放` 语句：按 `order_id` 且 `status = 已预占` 执行逻辑删除；
  - 明确并发单一终态：选择“退款分支”作为默认处理；
  - 对“三方状态机、幂等唯一键、超时重试数值”说明为“此前题目已回答，不再重复”。
- **导师复评（Q2追答）**：
  - 本次补充修正了实扣方向错误，并补齐了单一终态选择，分数上调为 `5/10`；
  - 仍未达标点：
    1. `预占 -> 释放` 仅逻辑删除不足以完成库存回补，缺少库存与预占字段的条件更新闭环；
    2. 本题要求的三方状态机、幂等去重主键/唯一键、超时与重试上限仍未在本题内显式给出。
- **学生补充（Q2二次追答）**：
  - 补充 `预占 -> 释放` 的库存回补流程：
    1. 先锁定库存-订单关联记录并读取预占数量；
    2. 将关联记录逻辑删除（条件：`order_id` + `status = 已预占`）；
    3. 执行库存表预占回减：`预占库存 = 预占库存 - 订单预占库存数量`。
- **导师二次复评（Q2二次追答）**：
  - 本次补充已覆盖“释放分支包含库存预占回减”的关键缺口，分数上调为 `6/10`；
  - 仍需补齐：
    1. `预占 -> 释放` 仍缺少条件保护（如预占下界、版本号或条件更新）以防并发下出现负数；
    2. 本题要求的三方状态机、幂等去重主键/唯一键、超时与重试上限仍未在本题内显式展开。
- **学生补充（Q2三次追答-历史回填）**：
  - 三方状态机（回填近几日已答内容）：
    1. 订单状态：`待付款 -> 待发货 -> 已发货 -> 待评价 -> 完成`，并包含 `已取消（含超时取消/库存异常）`；
    2. 支付状态：`待支付 -> 已支付/已退款`；
    3. 库存状态：预占库存与订单关联，状态显式为 `预占/已占`，并在支付成功时转实扣、超时取消时释放。
  - 幂等去重主键/唯一键（回填近几日已答内容）：
    1. 入口幂等：参数哈希作为幂等键，叠加 `user_id` 维度分布式锁；
    2. 消费幂等：库存/优惠券消费者均以 `order_id` 作为幂等业务键；
    3. 去重存储：使用 DB 消费记录表按业务ID建立索引，缓存幂等标记作为并发兜底；
    4. 业务键范围：`orderId/inventoryId/paymentId` 或消息唯一ID用于消息防重。
  - 超时与重试上限（回填近几日已答内容）：
    1. 库存与营销服务调用超时 `2秒`，重试最多 `3次`（可配置）；
    2. 调用链预算示例：`网关->订单 2000ms`、`订单->库存 500ms`、`订单->优惠券 500ms`，订单自身预留 `1000ms`。
- **导师三次复评（Q2三次追答）**：
  - 已完成本题中“三方状态机、幂等去重键、超时与重试上限”的历史答案回填，分数上调为 `8/10`；
  - 剩余扣分点聚焦一个：`预占 -> 释放` 需要补上并发条件保护（如版本号/条件更新）以避免并发负数或重复释放。
- **学生补充（Q2四次追答）**：
  - 补充并发保护：`select 预占库存数量 from inventory_order where order_id = #{order_id} for update`。
- **导师四次复评（Q2四次追答）**：
  - 本次补充已覆盖释放链路的并发保护要求（悲观锁实现），Q2 上调为 `10/10`；
  - 该题目所要求的状态机、幂等去重键、超时重试与并发保护已补齐。
- **知识点标签**：并发裁决, 条件更新SQL, 预占转实扣, 超时取消, 支付回调

---

## 会话总结

- **本次答题数**：2
- **总分**：20/20 = 100%（Q1: 10/10，Q2: 10/10）
- **结果**：✅ 满分
- **本次提升**：
  1. 通信边界与支付拆分意识明显提升
  2. 回调+回查双保险思路正确
- **仍需强化**：
  1. 可继续沉淀“悲观锁 vs 乐观锁（版本号）”的选型准则，形成统一模板
- **建议下一步**：进入下一题，继续做“高并发下库存锁粒度与吞吐权衡”专项。
- **等级变动**：L1 → L1（维持）

---

*保存时间：2026-03-21*
